# 实验室仪器共享管理系统 — 前端执行计划

> 基于 `lab-instrument-frontend`（Vue 3 + TypeScript + Vite + Ant Design Vue 4.x）  
> 配合后端 REST API `http://localhost:8000/api`，JWT 认证  
> 参考文档：`设计方案.md`（UI/UX）、`API文档-前端.md`（接口）、`AGENTS.md`（项目规约）

---

## 一、现状摘要

| 模块 | 状态 |
|------|------|
| `src/api/` | ✅ **已完成** — http.ts（axios 实例+拦截器）、auth.ts、instrument.ts、reservation.ts |
| `src/types/index.ts` | ✅ **已完成** — 全部 TypeScript 接口 |
| `src/main.ts` | ⚠️ 骨架 — 仅 `createApp(App).mount('#app')` |
| `src/App.vue` | ⚠️ 骨架 — 仅渲染 HelloWorld |
| `src/style.css` | ⚠️ Vite 默认样式（需替换为 Ant Design 主题变量） |
| `src/components/` | ❌ 仅 HelloWorld.vue（占位组件，需替换） |
| `src/layouts/` | ❌ 空目录 |
| `src/router/` | ❌ 空目录 |
| `src/stores/` | ❌ 空目录 |
| `src/views/auth/` | ❌ 空目录 |
| `src/views/instrument/` | ❌ 空目录 |
| `src/views/reservation/` | ❌ 空目录 |
| `src/views/admin/` | ❌ 空目录 |

---

## 二、总体阶段划分

```
Phase 1: 基础设施搭建（路由 + 状态管理 + 布局）
    ↓
Phase 2: 认证模块（登录 / 注册 / 登出）
    ↓
Phase 3: 仪器浏览模块（大厅卡片 + 详情 + 搜索筛选）
    ↓
Phase 4: 预约模块（创建预约 + 我的预约）
    ↓
Phase 5: 管理员后台（仪器管理 CRUD + 审批预约）
    ↓
Phase 6: 收尾打磨（导航守卫、错误处理、空白状态、构建验证）
```

每个 Phase 构建在前一个之上，可独立验证。

---

## Phase 1 — 基础设施搭建

**目标：** 搭建应用骨架 — Vue Router、Pinia stores、整体布局、Ant Design 全局注册，使页面可以在路由间跳转。

### 1.1 安装依赖

确认 `ant-design-vue`、`pinia`、`vue-router`、`@ant-design/icons-vue`、`dayjs` 已在 `package.json` 中（目前已声明，运行 `npm install` 确保装好）。

### 1.2 注册 Ant Design Vue（全局）

修改 `src/main.ts`：
- 引入 `Antd` 插件并 `app.use(Antd)`
- 引入 `Pinia` 并 `app.use(createPinia())`
- 引入路由实例并 `app.use(router)`

### 1.3 创建 Pinia Stores

**`src/stores/auth.ts`** — 用户认证 Store：
- `state`: `user: User | null`, `token: string | null`
- `getters`: `isLoggedIn`, `role`, `isAdmin`, `isStudent`, `isTeacher`
- `actions`: `login()`, `register()`, `logout()`, `loadFromStorage()`（页面刷新时从 localStorage 恢复）

**`src/stores/instrument.ts`** — 仪器 Store：
- `state`: `instruments: Instrument[]`, `categories: Category[]`, `loading: boolean`
- `actions`: `fetchInstruments()`, `fetchCategories()`, `fetchInstrumentById()`

**`src/stores/reservation.ts`** — 预约 Store：
- `state`: `reservations: Reservation[]`, `loading: boolean`
- `actions`: `fetchReservations()`, `createReservation()`, `cancelReservation()`, `approveReservation()`

### 1.4 创建布局组件

**`src/layouts/DefaultLayout.vue`** — 通用后台布局（设计方案 4.0）：
- 左侧导航栏（Ant Design `<a-layout-sider>`），根据角色动态渲染菜单
- 顶部顶栏（`<a-layout-header>`），显示系统标题 + 用户信息 + 登出按钮
- 右下工作区（`<a-layout-content>`），`<router-view />`
- 响应式：移动端侧栏可折叠

**`src/layouts/AuthLayout.vue`** — 登录注册专用布局：
- 左右分栏（左侧图片背景 + 蒙层，右侧白色表单面板）
- 适用于登录页和注册页

### 1.5 创建路由配置

**`src/router/index.ts`**：
```
/login          → AuthLayout > LoginView     (公开)
/register       → AuthLayout > RegisterView  (公开)
/instruments    → DefaultLayout > InstrumentListView   (需登录)
/instruments/:id → DefaultLayout > InstrumentDetailView (需登录)
/instruments/:id/reserve → DefaultLayout > ReservationCreateView (需登录)
/reservations   → DefaultLayout > ReservationListView   (需登录)
/admin          → DefaultLayout > AdminDashboardView    (管理员)
/admin/instruments → DefaultLayout > AdminInstrumentView (管理员)
/admin/reservations → DefaultLayout > AdminReservationView (管理员)
/               → 重定向到 /instruments
```

### 1.6 配置路由守卫

在 `src/router/index.ts` 中添加全局前置守卫：
- 未登录访问需鉴权路由 → 重定向 `/login`
- 非管理员访问 `admin/*` → 重定向 `/instruments`
- 已登录访问 `/login` 或 `/register` → 重定向 `/instruments`

### 1.7 替换 `src/style.css`

用 Ant Design 主题变量覆盖方案（参考 `设计方案.md` 配色）：
- 主色 `#1890FF`（科技蓝）
- 中性色：背景 `#F0F2F5`，卡片 `#FFFFFF`，正文 `#1F2937`
- 语义色：绿 `#52C41A`、红 `#F5222D`、橙 `#FAAD14`
- 保留 `prefers-color-scheme` 暗色模式
- 移除 Vite 默认模板样式

### 1.8 更新 `App.vue`

简化根组件：
- 删除 HelloWorld 引用
- 使用 `<a-config-provider>` 包裹 `<router-view />`（可选，用于全局主题）

### ✅ Phase 1 验证标准

构建通过：`npm run build` 无错误  
页面可在各路由间正常跳转（即使内容为空）  
未登录访问受保护页面自动跳转登录页

---

## Phase 2 — 认证模块

**目标：** 完整的登录 / 注册 / 登出流程，Token 持久化与自动恢复。

### 2.1 登录页面 `src/views/auth/LoginView.vue`

- 左右分栏布局（复刻 `AuthLayout` 或内联实现）
- 表单：用户名 + 密码（密码框带"睁眼/闭眼"切换图标）
- 角色切换：顶部 Segmented（学生 / 教师，管理员不可在此注册）
- 提交按钮：loading 状态
- 校验：必填项 + 最小长度
- 成功 → 存储 token+user 到 localStorage，调用 auth store，跳转 `/instruments`
- 失败 → message.error 展示后端错误信息
- 底部链接："没有账号？立即注册"

### 2.2 注册页面 `src/views/auth/RegisterView.vue`

- 左右分栏布局（与登录风格一致）
- 表单：用户名、密码、确认密码、真实姓名、学号/工号（可选）、院系（可选）、电话（可选）、邮箱（可选）
- 角色选择：Segmented（学生 / 教师）
- 校验：密码与确认密码一致
- 成功 → message.success，跳转登录页
- 失败 → 展示错误信息

### 2.3 导航栏用户菜单

在 `DefaultLayout.vue` 顶部顶栏：
- 显示当前用户信息（头像 + 姓名 + 角色标签）
- 下拉菜单：退出登录
- 退出登录：调用 API → 清除 localStorage → 跳转 `/login`

### ✅ Phase 2 验证标准

注册新用户 → 成功提示 → 跳转登录  
登录 → 跳转仪器大厅，顶栏显示用户信息  
刷新页面 → Token 恢复，保持登录状态  
登出 → 清除状态，跳转登录页，访问受保护页面被拦截

---

## Phase 3 — 仪器浏览模块

**目标：** 学生/教师可浏览仪器列表、查看详情、搜索筛选。

### 3.1 仪器大厅 `src/views/instrument/InstrumentListView.vue`

- **顶部搜索过滤区**（设计方案 4.2）：
  - 分类下拉（`<a-select>`，数据来自 `instrumentApi.getCategories()`）
  - 状态胶囊标签（`<a-radio-group>` 按钮样式：全部 / 空闲 / 使用中 / 维修中）
  - 关键词输入框
- **卡片栅格布局**（`<a-row>` + `<a-col>` + `<a-card>`）：
  - 每张卡片展示：状态指示灯（右上角 Tag）、名称（大字重）、型号、资产编号（等宽字体）、存放地点（带定位图标）
  - 卡片底部："查看详情"按钮 + "立即预约"按钮（非空闲时置灰+禁用）
- 加载态：`<a-skeleton>` 骨架屏
- 空态：无仪器时展示空状态插图 + 提示文字
- 分页（可选，根据 API 是否支持）

### 3.2 仪器详情页 `src/views/instrument/InstrumentDetailView.vue`

- 从路由参数 `:id` 获取仪器 ID
- 展示完整信息：名称、型号、资产编号、分类、存放地点、状态、描述
- "预约此仪器"按钮（仅空闲状态可点击）
- 底部"返回列表"链接

### ✅ Phase 3 验证标准

列表页展示仪器卡片  
按分类 / 状态 / 关键词筛选正常工作  
点击卡片进入详情页  
空闲仪器可点击预约按钮跳转预约页  
非空闲仪器预约按钮置灰

---

## Phase 4 — 预约模块

**目标：** 用户可创建预约、查看我的预约、取消预约。

### 4.1 创建预约 `src/views/reservation/ReservationCreateView.vue`

- 从路由参数或 store 获取仪器信息
- **日期选择**：`<a-calendar>` 或 `<a-date-picker>`（设计方案 4.4）
- **时段选择**：显示 4 个时段卡片（08:00-10:00 / 10:00-12:00 / 14:00-16:00 / 16:00-18:00）
  - 已被他人预约的时段：灰色+禁用（需 API 提供某日某仪器的已预约时段）
  - 选中：高亮蓝色边框
- 底部悬浮操作栏：显示仪器名称 + 日期+时段摘要 + "提交预约"按钮
- 提交成功 → message.success → 跳转"我的预约"

### 4.2 我的预约 `src/views/reservation/ReservationListView.vue`

- Tabs 切换（全部 / 待审批 / 已通过 / 已拒绝 / 已取消）
- 预约卡片列表：
  - 仪器名称、日期、时段（加粗）
  - 状态 Tag（绿=已通过、红=已拒绝、橙=待审批、灰=已取消）
  - 申请时间
  - 仅 "待审批" 状态显示"取消预约"按钮（二次确认 Popconfirm）
- 空态展示

### ✅ Phase 4 验证标准

选择仪器 → 选择日期+时段 → 提交预约 → 成功提示 → 跳转我的预约  
我的预约列表显示正确的状态 Tag  
取消预约需二次确认 → 取消后状态变为"已取消"

---

## Phase 5 — 管理员后台

**目标：** 管理员的仪器 CRUD + 预约审批。

### 5.1 仪器管理 `src/views/admin/AdminInstrumentView.vue`

- **数据表格**（`<a-table>`）展示所有仪器（设计方案 4.3）：
  - 列：ID、名称、型号、资产编号、分类、存放地点、状态（带背景色 Tag）、操作
  - 支持排序（按 ID、名称）
  - 操作列："编辑"、"删除"文字按钮
- **新增仪器**：点击"新增"按钮 → 弹出 Modal/Drawer 表单：
  - 名称（必填）、型号、资产编号、分类（下拉，必填）、存放地点、描述
  - 提交 → 刷新列表
- **编辑仪器**：点击"编辑" → 弹出 Modal/Drawer 预填数据 → 保存
- **删除仪器**：点击"删除" → Popconfirm 二次确认 → 删除 → 刷新列表
- 加载态：表格自带 loading

### 5.2 预约审批 `src/views/admin/AdminReservationView.vue`

- **数据表格**展示所有预约（管理员可见全部）：
  - 列：ID、用户、仪器、日期、时段、状态、申请时间、备注、操作
  - 状态 Tag
  - 操作列："通过"（绿色）、"拒绝"（红色）
- 点击"通过"/"拒绝" → 可选输入备注 → 确认 → 刷新列表
- 筛选：按状态、日期范围

### ✅ Phase 5 验证标准

管理员菜单显示"仪器管理"和"预约审批"  
新增/编辑/删除仪器正常  
审批通过/拒绝预约正常  
学生/教师看不到管理员菜单项

---

## Phase 6 — 收尾打磨

**目标：** 全局守卫、错误边界、加载态、空白态覆盖，构建验证。

### 6.1 全局错误处理

- 完善 `http.ts` 响应拦截器：401 重定向、403 权限提示、500 通用提示
- 全局 `onErrorCaptured` / `errorHandler` 捕捉 Vue 运行时错误

### 6.2 路由守卫增强

- 页面加载前检查 Token 有效性（可配合 axios 请求）
- 角色权限路由元信息 `meta: { roles: ['admin'] }`

### 6.3 各页面空态 / 加载态 / 错误态

- 每个列表页添加 `<a-empty>` 空状态
- 加载中使用 `<a-spin>` 或 `<a-skeleton>`
- API 失败时展示错误提示，不白屏

### 6.4 构建验证

```bash
npm run build    # vue-tsc 类型检查 + vite build
npm run preview  # 预览生产构建
```

- 清除所有 `any` 类型（如果可能）
- 确保 `noUnusedLocals`、`noUnusedParameters` 不报错

### ✅ Phase 6 验证标准

`npm run build` 零错误零警告  
所有页面覆盖加载态、空态、错误态  
路由守卫拦截未授权访问  
401 自动跳转登录

---

## 三、依赖关系图

```
Phase 1 ───→ Phase 2 ───→ Phase 3 ───→ Phase 4
                                                ↓
                                          Phase 5
                                                ↓
                                          Phase 6
```

- Phase 1 是所有后续阶段的前提
- Phase 2 是 Phase 3~5 的前提（需要登录态）
- Phase 3 是 Phase 4 的前提（需要仪器数据才能预约）
- Phase 5 可独立于 Phase 3/4 开发（但需要 Phase 1+2 的基础）
- Phase 6 是全局收尾，可在任意阶段并行推进

---

## 四、关键技术决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 全局组件库 | Ant Design Vue 4.x | 设计规范指定，B端后台成熟方案 |
| 状态管理 | Pinia (Composition API 风格) | Vue 3 官方推荐，与 setup 语法天然配合 |
| 路由 | Vue Router 5 + 全局守卫 | 需要角色鉴权，集中式守卫更可控 |
| HTTP 客户端 | Axios (已有封装) | 拦截器统一处理 Token/401 |
| 页面布局 | 左侧导航 + 顶部顶栏 + 内容区 | 经典 B 端布局，设计方案明确 |
| CSS 方案 | 全局 CSS 变量 + 组件 Scoped 样式 | 现有风格继承，Ant Design 主题覆盖 |
| Token 存储 | localStorage | 简单可靠，API 文档推荐 |
| 日期处理 | dayjs | 已有依赖，轻量 |

---

## 五、交付清单

```
lab-instrument-frontend/
├── src/
│   ├── main.ts                      ← 更新：注册 Antd + Pinia + Router
│   ├── App.vue                      ← 更新：替换为路由视图
│   ├── style.css                    ← 更新：Ant Design 主题变量
│   ├── router/
│   │   └── index.ts                 ← 新建：路由表 + 守卫
│   ├── stores/
│   │   ├── auth.ts                  ← 新建：认证状态管理
│   │   ├── instrument.ts            ← 新建：仪器状态管理
│   │   └── reservation.ts           ← 新建：预约状态管理
│   ├── layouts/
│   │   ├── DefaultLayout.vue        ← 新建：通用后台布局
│   │   └── AuthLayout.vue           ← 新建：登录注册布局
│   ├── views/
│   │   ├── auth/
│   │   │   ├── LoginView.vue        ← 新建
│   │   │   └── RegisterView.vue     ← 新建
│   │   ├── instrument/
│   │   │   ├── InstrumentListView.vue    ← 新建
│   │   │   └── InstrumentDetailView.vue  ← 新建
│   │   ├── reservation/
│   │   │   ├── ReservationCreateView.vue ← 新建
│   │   │   └── ReservationListView.vue   ← 新建
│   │   └── admin/
│   │       ├── AdminInstrumentView.vue   ← 新建
│   │       └── AdminReservationView.vue  ← 新建
│   └── components/                  ← 可选：抽取复用组件
│       └── ... (卡片、状态标签等)
```

共约 **16 个新文件 / 4 个修改文件**，分 6 个阶段实施。
