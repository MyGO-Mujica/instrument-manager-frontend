这是一套为“桂林电子科技大学实验室仪器共享管理系统”量身定制的前端UI/UX设计规范与方案。基于高校实验室管理的需求，设计方案将侧重于**严谨、高效、清晰**的B端（企业级）后台管理系统风格。

---

### 一、 整体设计调性与原则

* **整体调性：** 学术、专业、现代、极简。视觉上应传达出工科院校的严谨性与科技感，避免过度装饰，以内容展示和任务效率为核心。
* **设计原则：**
* **信息层级分明：** 仪器参数、状态、时间段等复杂信息需通过排版与留白清晰区分。
* **状态可视化：** 强依赖色彩和图标来快速传递仪器的当前状态（空闲/使用中/维修）和订单状态。
* **操作提效：** 预约流程需具备防呆设计（如冲突时间段置灰不可选），表单填写支持键盘快捷切换。
* **角色一致性与差异化：** 整体UI框架统一，但通过侧边栏菜单的动态渲染，实现不同角色（学生/教师/管理员）的权限隔离。



---

### 二、 核心配色方案 (Color Palette)

配色需结合“桂电”的校园属性与系统功能性，建议采用**科技蓝**作为主色调，辅以标准的语义色。

* **主色调 (Primary)：** `科技蓝 #1890FF` 或 `桂电校徽蓝 #004B87`（作为顶部导航条、主按钮、激活状态的颜色）。
* **中性色 (Neutral)：**
* 背景色：`浅灰白 #F0F2F5`（用于全局背景，区分内容卡片）。
* 卡片与面板底色：`纯白 #FFFFFF`。
* 正文主标题/强调文字：`深灰 #1F2937`。
* 次要/辅助说明文字：`浅灰 #6B7280`。
* 边框与分割线：`极浅灰 #E5E7EB`。


* **语义与状态色 (Semantic & Status)：**
* **🟢 可用/通过 (Available / Approved)：** `常春藤绿 #52C41A`（用于仪器空闲状态、审批通过）。
* **🔴 使用中/拒绝 (In Use / Rejected)：** `警示红 #F5222D`（用于仪器使用中状态、审批驳回）。
* **🟠 维修中/待审批 (Maintenance / Pending)：** `活力橙 #FAAD14`（用于仪器维修状态、订单待审批）。



---

### 三、 字体与排版规范 (Typography)

* **字体家族 (Font-Family)：** 首选系统默认无衬线字体以保证加载速度与跨平台一致性。
* *中文：* `PingFang SC`, `Microsoft YaHei`, `Noto Sans SC`。
* *英文/数字：* `Inter`, `Roboto`, 或 `Helvetica Neue`（资产编号和时间显示需使用等宽字体特征，如 `Consolas` 以利于对齐）。


* **字号阶梯 (Font Sizes)：**
* 系统标题/页面大标：`24px / 20px` (加粗)
* 卡片标题/模块标：`16px` (加粗)
* 正文常规阅读：`14px` (常规)
* 辅助提示/标签：`12px` (常规)



---

### 四、 核心页面视觉与布局规范

建议采用经典的**左侧导航栏 + 顶部顶栏 + 右下工作区**的布局（Dashboard 布局），确保屏幕空间的最高利用率。

#### 4.1 登录与注册模块

* **视觉布局：** 采用左右分栏设计。左侧展示桂电校园风光或实验室相关的插画/图片（带有深蓝色半透明蒙层），右侧为纯白色的表单面板。
* **设计元素：**
* 角色切换采用顶部的 `Segmented Control`（分段控制器：学生 / 教师）。
* 输入框采用大尺寸（Height: 40px），聚焦时边框高亮为主色。
* 密码输入框必须包含“闭眼/睁眼”的切换图标。



#### 4.2 仪器信息大厅 (面向学生/教师)

* **视觉布局：** 顶部为复合搜索区，下方为数据展示区。
* **搜索过滤区：** * 提供“分类”（下拉选框）、“状态”（胶囊标签切换单选）、“关键词”（输入框）。
* **展示形式：** 采用**卡片式栅格布局 (Card Grid)**。
* 每张卡片代表一台仪器。
* 卡片顶部右上角放置**状态指示灯**（例如：🟢 空闲）。
* 卡片主体包含：仪器名称（大字重）、型号、资产编号（等宽浅色字）、存放地点（带定位小图标）。
* 卡片底部放置操作区：“查看详情”与“立即预约”按钮（非空闲状态时，预约按钮需置灰并显示为 `不可选`）。



#### 4.3 仪器管理模块 (面向管理员)

* **展示形式：** 采用**复杂数据表格 (Data Table)**。
* **设计元素：**
* 支持列宽拖拽与表头排序。
* 状态列必须使用带背景色的 Tag 标签（如红底红字的“使用中”标签）。
* 操作列提供文字按钮（编辑、删除、状态流转），避免使用过多图标导致认知负担。
* 新增/编辑仪器采用**右侧抽屉 (Drawer)** 或 **居中模态框 (Modal)**，避免页面跳转打断工作流。



#### 4.4 预约订单管理模块

* **预约交互设计 (重点)：**
* **时间选择器：** 采用**日历+时间轴 (Calendar & Timeline)** 的组合。
* 左侧显示日期选择（日历组件），右侧显示该日期的固定时段（如 08:00-10:00, 10:00-12:00）。
* 已被他人预约的时段，其区块背景色变为浅红色或灰色，并带有 `禁用斜纹图案` 或锁定图标。
* 用户点击空闲时段块，即呈现选中高亮状态，底部弹出悬浮操作条提交预约。


* **我的订单/审批页面：**
* 提供 Tabs 标签页切换订单状态（全部 / 待审批 / 已通过 / 已取消）。
* 订单列表需清晰展示：仪器缩略信息、预约时间段（使用加粗数字强调）、当前申请状态。



---

### 五、 UI 组件与反馈设计规范

* **按钮 (Buttons)：**
* 主要操作（提交、保存、预约）：实心主色按钮。
* 次要操作（取消、重置）：描边按钮或浅色背景按钮。
* 破坏性操作（删除、取消预约）：红色警告按钮。


* **提示反馈 (Feedback)：**
* 操作成功/失败：在页面顶部中央弹出轻量级的 `Toast/Message` 提示，停留 3 秒后自动消失。
* 删除等高危操作：必须弹出带有“遮罩层”的二次确认气泡框（Popconfirm）。


* **加载状态 (Loading)：**
* 在请求 API 时，按钮内部需显示旋转的 loading 图标，表格数据加载时需有骨架屏 (Skeleton) 或半透明白底加转圈动画，避免用户重复点击。



